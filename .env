# ─── Общие ─────────────────────────────────────────────────────────────────────
DOMAIN_NAME=login.42.fr         # твой логин: login.42.fr

# ─── MariaDB (использует create_db.sh) ─────────────────────────────────────────
DB_NAME=wordpress
DB_USER=wp_user
DB_USER_PASS=change_me_wp_user
DB_ROOT_PASS=change_me_root

# ─── WordPress (подхватывает твой setup/entrypoint WordPress) ──────────────────
# Хост БД внутри docker-сети:
WORDPRESS_DB_HOST=mariadb:3306
WORDPRESS_DB_NAME=${DB_NAME}
WORDPRESS_DB_USER=${DB_USER}
WORDPRESS_DB_PASSWORD=${DB_USER_PASS}

# Админ WP — имя НЕ должно содержать "admin"/"Admin"
WP_ADMIN_USER=site_owner
WP_ADMIN_PASS=change_me_admin
WP_ADMIN_EMAIL=owner@example.com
WP_TITLE=My WP
WP_URL=https://${DOMAIN_NAME}

# ─── Nginx / TLS (если в Dockerfile генерируешь self-signed) ──────────────────
# Можно оставить пустыми, если генеришь без переменных:
SSL_COUNTRY=AE
SSL_STATE=Dubai
SSL_LOCALITY=Dubai
SSL_ORG=Inception
SSL_CN=${DOMAIN_NAME}